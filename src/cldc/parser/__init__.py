"""Rule parser layer for cldc.

The actual parser lives in `cldc.parser.rule_parser`. This module is kept
empty so the package import surface stays explicit; import directly from
`cldc.parser.rule_parser`.
"""
