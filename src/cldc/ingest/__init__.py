"""Repository ingest layer for cldc.

Submodules:

- `cldc.ingest.discovery`     repo-root discovery and source inventory.
- `cldc.ingest.source_loader` canonical source loading + `extends:` resolution.
"""
