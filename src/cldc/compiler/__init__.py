"""Compiler layer for cldc.

The actual compiler lives in `cldc.compiler.policy_compiler`. This module is
kept empty so the package import surface stays explicit; import directly from
`cldc.compiler.policy_compiler`.
"""
